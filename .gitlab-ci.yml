##############################################################################
### Autogenerated with R package kwb.pkgbuild v0.0.0.9000
### (installed from 'Github (kwb-r/kwb.pkgbuild@6117d6f)' source code on 2019-01-28)
### by calling the function kwb.pkgbuild::use_gitlab_ci_pkgdown()
### (file created at: 2019-01-31 11:06:15)
##############################################################################


### Modified from template from https://bookdown.org/yihui/blogdown/gitlab-pages.html
### https://raw.githubusercontent.com/rstudio/blogdown/88a05670a52c4f512c6ac9fbad0242a4d7ccde15/docs/03-deploy.Rmd

image: debian:buster-slim

before_script:
  - apt-get update && apt-get -y install pandoc r-base libcurl4-openssl-dev libxml2-dev libssl-dev
  - R -e "install.packages(c('pkgdown','remotes'), repos='http://cran.rstudio.com')"
  - R -e "remotes::install_github('kwb-r/kwb.package', dependencies = TRUE, upgrade = 'always')"

pages:
  script:
    - R -e "pkgdown::build_site()"
    - mv docs public
  artifacts:
    paths:
      - public
  only:
  - master
